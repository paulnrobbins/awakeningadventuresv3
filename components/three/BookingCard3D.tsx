'use client';

/**
 * Retained as a stub so any old import doesn't break the build. The
 * card stack now lives in SceneBook as DOM tiles for production
 * stability. drei <Html transform> mode is a known crash point in
 * minified production builds.
 */
export function BookingCard3D() {
  return null;
}
